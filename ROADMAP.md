# AnimeHunt — ROADMAP

> Шкала готовности: **1% → 100%** (100% = задеплоен на хостинг и готов к реальным пользователям)
>
> Приоритеты внутри каждого блока:
> - 🔴 **Критическое** — без этого блок нельзя считать завершённым
> - 🟠 **Важное** — сильно влияет на качество / безопасность
> - 🟡 **Нужное** — заметно улучшает опыт
> - 🔵 **Потом** — хорошо иметь, но не блокирует

---

## 📍 Текущий статус проекта: ~87%

Проект имеет полноценное ядро: аутентификация, каталог аниме и персонажей с фильтрами, страницы аниме и персонажей, система оценок/рецензий/комментариев с форматированием, медиа-галерея с модерацией, шипперинг, избранное, профили пользователей с приватностью, admin-панель. Работает локально на `localhost`. До продакшена не хватает: security-хардения, работы с переменными окружения, оптимизации, SEO, CI/CD и непосредственно деплоя.

---

## Этап 1 — Ядро и MVP (1% → 25%) ✅ ЗАВЕРШЁН

### 🔴 Критическое
- [x] Схема базы данных PostgreSQL (пользователи, аниме, персонажи, жанры, студии)
- [x] REST API на Express: CRUD аниме, авторизация (register/login/JWT)
- [x] React SPA: роутинг, AuthContext, защищённые маршруты
- [x] Каталог аниме с поиском, фильтрами (статус, тип, жанр, студия, тема)
- [x] Страница аниме: описание, инфо-сайдбар, персонажи, авторы
- [x] Регистрация и вход, JWT в localStorage

### 🟠 Важное
- [x] Роли пользователей (admin / moderator / user)
- [x] Система оценок аниме (1–10), средняя оценка
- [x] Список просмотра (Смотрю / Просмотрено / Запланировано) с прогрессом

### 🟡 Нужное
- [x] Главная страница с новинками и топом
- [x] Адаптивная шапка с навигацией

### 🔵 Потом
- [x] Страница 404 / обработка ошибок на фронте

---

## Этап 2 — Контент и сообщество (25% → 50%) ✅ ЗАВЕРШЁН

### 🔴 Критическое
- [x] Система персонажей: каталог, страница персонажа, привязка к нескольким аниме (character_appearances)
- [x] Комментарии к аниме и персонажам: древовидные ответы, форматирование (**bold**, _italic_, ~~strike~~, __underline__, [spoiler])
- [x] Рецензии к аниме: заголовок, текст ≥100 символов, оценка, лайки
- [x] Серии/сезоны: аниме объединяются в серии через anime_series_entries

### 🟠 Важное
- [x] Кадры и видео: загрузка пользователями, модерация (approve/reject)
- [x] Возрастные рейтинги (G / PG / PG-13 / R-17 / R+)
- [x] Темы аниме (отдельная таблица anime_themes)
- [x] Диаграмма распределения оценок (гистограмма + donut chart)
- [x] Шипперинг персонажей: голосование, двусторонний топ с прогресс-барами

### 🟡 Нужное
- [x] Избранные персонажи + счётчик
- [x] Профиль пользователя: аватар, настройки приватности (hide_email, hide_watchlist)
- [x] Публичные профили по URL `/profile/:username`
- [x] Прикрепление изображений к комментариям (base64, до 5 МБ)
- [x] Глобальный поиск в шапке (аниме + персонажи, дебаунс)

### 🔵 Потом
- [x] Admin-панель: добавление/редактирование аниме и персонажей
- [x] Форма редактирования персонажа (только admin)
- [x] Привязка аниме к серии прямо из формы
- [x] Кликабельные ники пользователей → их профиль
- [x] Аватары отображаются в комментариях

---

## Этап 3 — Качество и безопасность (50% → 70%) 🔄 В ПРОЦЕССЕ (~80% этапа)

### 🔴 Критическое
- [x] **Вынести все секреты в `.env`** — сейчас `JWT_SECRET=supersecretkey123` и `DB_PASSWORD=postgres` (менять перед деплоем обязательно)
- [x] **Убрать хардкод `localhost:3001`** из `src/api/api.js` и `src/pages/Profile.jsx` → использовать `VITE_API_URL` из `.env`
- [x] **CORS**: заменить `origin: "http://localhost:5173"` на переменную окружения
- [x] **Helmet.js** — HTTP security headers (X-Frame-Options, CSP, HSTS и др.) *(исправлен crossOriginResourcePolicy для uploads)*
- [x] **Rate limiting** — `express-rate-limit` на `/api/auth/*` (защита от брутфорса)
- [x] **Валидация входных данных** на сервере — добавить `joi` или `zod` для всех POST/PUT роутов
- [x] **Создать `.env.example`** — шаблон с описанием всех переменных (без реальных значений)
- [x] **Сжатие ответов** — `compression` middleware

### 🟠 Важное
- [x] **Пагинация в каталоге** — 24 тайтла на страницу, кнопки навигации с ellipsis
- [x] **SQL-инъекции** — уже используется параметризация, но нужен audit всех запросов (параметризация подтверждена во всех динамических WHERE) (особенно динамические WHERE)
- [x] **Размер загружаемых изображений** — сжатие через `sharp` перед сохранением: `multer.memoryStorage()` + `sharp` pipeline: resize ≤1920px, конвертация в WebP (quality 82), GIF сохраняется как есть (анимация). Лимит загрузки поднят до 20 МБ, на диск пишется уже сжатый файл.
- [ ] **Хранилище изображений** — локальная папка `server/uploads/` не масштабируется, пока не трогаем;
- [x] **Refresh token** — access token 15 мин + refresh token 30 дней с ротацией. `POST /api/auth/refresh` (ротирует refresh, выдаёт новый access), `POST /api/auth/logout` (отзывает токен). Таблица `refresh_tokens` (миграция 005). Клиент: авто-рефреш при 401 в `api.js` (с защитой от параллельных запросов через `_refreshPromise`). Обратная совместимость — поле `token` сохранено.
- [x] **Логирование** — `morgan` для HTTP-логов, структурированные логи ошибок

### 🟡 Нужное
- [x] **Обработка ошибок** — глобальный error handler в Express; `Error Boundary` в React *(Express часть)*
- [x] **Загрузка состояний** — скелетоны вместо спиннеров в каталоге и на главной
- [x] **SEO**: заменить `<title>site</title>` на динамические мета-теги (название аниме/персонажа), Open Graph — хук `usePageMeta`, lang="ru" в index.html
- [x] **Смена пароля** — форма в настройках профиля с проверкой текущего пароля
- [x] **Удаление аккаунта** — возможность для пользователя (мягкое удаление, подтверждение паролем)
- [x] **Модерация комментариев** — мод/админ может удалять чужие комментарии (аниме + персонажи)
- [x] **Добавить списки отложено и дропнуто** — статусы `on_hold` (⏸) и `dropped` (✕) добавлены в API, UI и профиль
- [x] **Редактирование комментария** — автор может редактировать в течение 15 минут после публикации

### 🔵 Потом
- [ ] **Email-верификация** при регистрации (через nodemailer / Resend)
- [ ] **Восстановление пароля** по email
- [x] **Уведомления** (ответ на комментарий, лайк рецензии) — NotificationBell в шапке с polling, dropdown, пометка прочитанным, страница `/notifications` *(v8: исправления светлой темы; v9: иконка колокольчика теперь отображается в шапке для авторизованных пользователей, ссылка «Уведомления» добавлена в мобильное меню)*
- [x] **Экспорт списка** пользователя в CSV/JSON — кнопка «Экспорт» на вкладке «Списки» в профиле (видна только себе), выпадающее меню с форматами CSV (с BOM для Excel) и JSON; серверный эндпоинт `GET /api/watchlist/export?format=csv|json`; `aired_from` добавлен в GET /api/watchlist
- [ ] **Импорт из MyAnimeList / Shikimori** через их API

---

## Этап 4 — Полнота функций (70% → 82%)

### 🔴 Критическое
- [x] **Admin-панель — управление пользователями**: просмотр списка, смена роли, блокировка
- [x] **Admin-панель — управление жанрами и студиями**: добавление, редактирование, удаление
- [x] **Удаление аниме** (только admin) с каскадом — добавлен роут `DELETE /admin/anime/:id` с каскадным удалением
- [x] **Очередь на модерацию** в admin-панели — единый экран с pending кадрами и видео всех аниме

### 🟠 Важное
- [x] **Сэйю (сейю)** — API + UI форма на странице персонажа: поиск существующих, создание нового, привязка с выбором языка озвучки, удаление
- [x] **Авторы аниме** — API + UI форма во вкладке «Авторы» на странице аниме: поиск существующих, создание нового, привязка с выбором роли, удаление
- [x] **Поиск по каталогу персонажей**: фильтр по роли (главный/второстепенный/эпизодический), пагинация, `{items,total}` от API
- [x] **Страница серии** `/series/:id` — все части с навигацией, рейтинги, роут на бэке и фронте
- [x] **Расширенный профиль** — биография, соц. ссылки (VK/TG/Twitter/Discord/YouTube/MAL), баннер, локация, сайт — форма в настройках, отображение на публичном профиле
- [x] **История оценок** пользователя — SVG-график распределения, timeline по месяцам, список последних оценок; вкладка «Оценки» в профиле — запланировано

### 🟡 Нужное
- [x] **Рейтинг пользователей** по активности (комментарии, рецензии, оценки) — leaderboard в AdminPage
- [x] **Жалобы** на комментарии/рецензии — ReportModal с причинами, очередь в AdminPage, статусы pending/reviewed/dismissed
- [x] **Теги в комментариях** — @username парсится при отправке, уведомление типа mention отправляется адресату
- [x] **Закреплённые рецензии** от мода для аниме — is_pinned в reviews, PATCH /api/admin/reviews/:id/pin
- [x] **Фильтр каталога по сезону выхода** — выбор года + квартала (Зима/Весна/Лето/Осень) в фильтрах каталога, фильтр по URL

### 🔵 Потом
- [x] **Рекомендации** — «Похожие аниме» — вкладка «Похожие» на странице аниме; бэкенд `GET /api/anime/:id/similar` подбирает до 6 тайтлов по совпадению жанров (×3 балла), типу (+2) и студии (+1), исключает текущее аниме и тайтлы из той же серии; карточки с постером, рейтингом, типом и жанрами
- [ ] **Списки/подборки(Коллекции)** — пользователи создают свои топы
- [x] **Новости / блог** — таблица news в БД, CRUD-роут, блок на главной, управление в AdminPage *(v9: блок новостей переработан — featured-карточка + сетка, пагинация «Показать ещё», форма добавления новости с модалкой и превью обложки для модераторов и администраторов прямо на главной; `requireAdmin` → `requireModOrAdmin` в server/routes/news.js; v13: редактор новостей полностью переработан — тулбар форматирования **B/I/S/U/⚠️**, кнопка 🔗 с поисковым пикером аниме/персонажей/авторов, вставка тегов `[anime:id|Название]`, живое превью с рендерингом ссылок и форматирования; Home.jsx — форма добавления новостей получила тот же редактор; NewsBodyInline рендерит ссылки-теги как кликабельные `<a>` в карточках)*
- [x] **Тёмная/светлая тема** — CSS-переменные, ThemeContext, кнопка в шапке (🌙/☀), сохранение в localStorage *(исправлено: ThemeToggle теперь рендерится в шапке desktop+mobile; все жёстко прописанные цвета в Layout заменены на CSS-переменные)*

---

## Этап 5 — Тестирование (82% → 88%)

### 🔴 Критическое
- [ ] **Ручное тестирование** всех flow: регистрация → добавление в список → оценка → рецензия → комментарий
- [ ] **Тест безопасности**: попытки доступа без токена, подделка role_id в запросах, перебор JWT
- [ ] **Кросс-браузерное** тестирование: Chrome, Firefox, Safari, Edge

### 🟠 Важное
- [ ] **Интеграционные тесты** API (Jest + Supertest): auth, CRUD аниме, рейтинги
- [ ] **Тест производительности** — нагрузочный тест на популярные эндпоинты (k6 / Artillery)
- [ ] **Тест на мобильных** устройствах (iOS Safari, Android Chrome)

### 🟡 Нужное
- [ ] **Unit-тесты** утилит и хелперов (parseGenreIds, saveAnimeSeries и др.)
- [ ] **Тест на доступность** (a11y): aria-label, keyboard navigation

### 🔵 Потом
- [ ] **E2E тесты** (Playwright / Cypress) для критических сценариев
- [ ] **Visual regression** — скриншоты страниц для сравнения

---

## Этап 6 — Оптимизация (88% → 93%)

### 🔴 Критическое
- [x] **Индексы БД** — миграция `migrations/002_performance_indexes.sql`: индексы по всем FK, status, type, is_new, score, username, email
- [x] **Vite build** — code splitting через `React.lazy` + `Suspense` для всех страниц в App.jsx
- [x] **Оптимизация изображений** — `loading="lazy"` на всех img (AnimeCard, SeriesPage), WebP конвертация — TODO на этапе деплоя

### 🟠 Важное
- [ ] **Кэширование** — `Cache-Control` заголовки для статики; Redis для горячих запросов (топ аниме, жанры)
- [ ] **Сжатие ответов** сервера (gzip/brotli через nginx или compression middleware)
- [x] **Connection pooling** — настроен `max`, `idleTimeoutMillis`, `connectionTimeoutMillis`, SSL-флаг в db.js

### 🟡 Нужное
- [x] **Prefetch** следующей страницы каталога *(тихий prefetch через fetchAnimeList при изменении page, браузер кэширует запрос)*
- [x] **Service Worker** / PWA манифест — `public/sw.js` (cache-first статика, network-first API, push-задел), `public/manifest.json` с shortcuts, регистрация в `main.jsx` (только PROD), meta-теги в `index.html`
- [ ] **Дебаунс и memo** — аудит лишних ре-рендеров в каталоге

### 🔵 Потом
- [ ] **CDN** для статических ассетов (Cloudflare Pages / BunnyCDN)
- [ ] **SSR / SSG** — рассмотреть Next.js или Remix для SEO (большой рефакторинг)

---

## Этап 7 — Деплой (93% → 100%)

### 🔴 Критическое
- [ ] **Настроить переменные окружения** на хостинге (не коммитить `.env`)
- [ ] **БД на хостинге** — Supabase / Railway PostgreSQL / Neon, применить миграции
- [ ] **Деплой бэкенда** — Railway / Render / VPS (DigitalOcean, Hetzner); настроить `NODE_ENV=production`
- [ ] **Деплой фронтенда** — Vercel / Netlify / Cloudflare Pages; прописать `VITE_API_URL`
- [ ] **HTTPS** — SSL-сертификат (Let's Encrypt через nginx или автоматически у хостинга)
- [ ] **Настроить CORS** под продакшен-домен

### 🟠 Важное
- [ ] **Настроить nginx** в качестве reverse proxy (если VPS): upstream на Express, gzip, static files
- [ ] **Хранилище загрузок** — перенести `server/uploads/` в облако (иначе файлы пропадут при рестарте контейнера)
- [ ] **CI/CD pipeline** — GitHub Actions: lint → test → build → deploy при пуше в `main`
- [ ] **Мониторинг** — Sentry для ошибок фронта и бэка; UptimeRobot для доступности
- [ ] **Резервное копирование БД** — автоматический pg_dump по расписанию (cron)

### 🟡 Нужное
- [ ] **Домен** — купить и привязать к деплою
- [x] **robots.txt** — `public/robots.txt` с Disallow для /api, /admin; **sitemap.xml** — динамический эндпоинт `/sitemap.xml` на Express (`server/routes/sitemap.js`), берёт все аниме и персонажей из БД, Cache-Control 1ч
- [x] **Dockerfile + docker-compose** — multi-stage build (frontend-build → server), docker-compose с PostgreSQL + volumes (pg_data, uploads_data), healthcheck, SIGTERM/SIGINT graceful shutdown. Express раздаёт `dist/` в production + SPA fallback
- [x] **Health check endpoint** `/api/health` — для мониторинга *(возвращает `{ok:true, env}`, уже реализован в server/index.js)*
- [x] **Graceful shutdown** сервера — обработка SIGTERM и SIGINT: `server.close()` → `db.end()` → `process.exit(0)`, таймаут 10с на принудительный выход

### 🔵 Потом
- [ ] **Horizontal scaling** — несколько инстансов Express за балансировщиком
- [ ] **Rate limiting** на уровне nginx
- [ ] **DDoS защита** — Cloudflare прокси
- [ ] **Canary deployments** — постепенный роллаут новых версий
- [ ] **Changelog / релизы** в GitHub Releases

---

## Сводная таблица этапов

| Этап | Описание | Готовность |
|------|----------|------------|
| 1 | Ядро и MVP | ✅ 100% |
| 2 | Контент и сообщество | ✅ 100% |
| 3 | Качество и безопасность | ✅ 100% |
| 4 | Полнота функций | ✅ 100% |
| 5 | Тестирование | ⬜ 0% |
| 6 | Оптимизация | 🔄 ~75% |
| 7 | Деплой | 🔄 ~35% |

**Итого: ~90% от финального продакшен-релиза**

### 📝 Лог изменений
- **2026-05-08 v14**: **Профиль — исправление соцсетей.** Баг: браузер автоматически заполнял поле MAL email-адресом (browser autofill). Исправление: все поля соцсетей получили `autoComplete="off"`, `name="social_{net}"`. Добавлена функция `resolveSocialUrl(net, value)` — поддерживает @username или просто username: VK→vk.com, Telegram→t.me, Twitter→x.com, Discord→discord.com/users, YouTube→youtube.com/@, MAL→myanimelist.net/profile; полные URL передаются как есть. Метки сетей в форме читаемые, у каждой свой плейсхолдер. Иконка Twitter: TW→𝕏. **Roadmap: реализован пункт «Похожие аниме»** — вкладка «Похожие» на странице аниме; бэкенд GET /api/anime/:id/similar подбирает до 6 тайтлов по релевантности (жанры×3 + тип+2 + студия+1), исключает текущее и тайтлы из той же серии; `fetchSimilarAnime(id)` в api.js; компонент `SimilarAnimeTab` с постером, рейтингом, типом, жанрами.
- **2026-05-07 v13**: Редактор новостей полностью переработан. **AdminPage → NewsTab**: форма заменена компонентом `NewsEditor` с тулбаром форматирования (B/I/S/U/⚠️ spoiler, Ctrl+B/Ctrl+I), кнопкой «🔗 Ссылка» открывающей `NewsLinkPicker` — поиск по аниме/персонажам/авторам с дебаунсом 300мс, вставляет тег `[anime:id|Название]`/`[character:id|Имя]`/`[staff:id|Имя]`. Живое превью отображает итоговый текст с рендерингом ссылок и форматирования (`NewsBodyPreview`). **Home.jsx → AddNewsButton**: та же функциональность добавлена в модалку публикации новостей для модераторов/администраторов (`HomeLinkPicker`). Новый компонент `NewsBodyInline` в Home.jsx рендерит теги-ссылки как кликабельные `<a href="/anime/...">` прямо в карточках на главной странице. **server/routes/upload.js**: добавлено сжатие изображений через `sharp` — `multer.memoryStorage()` + resize ≤1920px + WebP (quality 82); GIF-файлы сохраняются без пересжатия; лимит загрузки увеличен до 20 МБ. Roadmap: закрыт пункт «Размер загружаемых изображений».
- **2026-05-03 v12**: Добавлено редактирование записей в списке просмотра прямо из профиля (компонент WatchEntry переработан): кнопка «✏️» появляется при наведении, открывает inline-панель с выбором статуса и регулировкой эпизодов (−/+ и кнопка «Все N»), прогресс-бар в реальном времени. Автоматический перевод аниме из «Смотрю» в «Просмотрено» при достижении максимального числа эпизодов — с подсказкой «→ будет перенесено в Просмотрено». Кнопка «✕» для удаления из списка с подтверждением. Обновления применяются локально без перезагрузки страницы. Roadmap: реализован **Экспорт списка** — кнопка «Экспорт (N)» с выпадающим меню форматов CSV и JSON; CSV с BOM для корректного открытия в Excel; серверный эндпоинт `GET /api/watchlist/export?format=csv|json`; `aired_from` добавлен в запрос GET /api/watchlist.
- **2026-05-02 v11**: Исправлена ошибка «столбец air_weekday не существует» при добавлении аниме — поля расписания (`air_weekday`, `air_time`, `episodes_aired`, `next_episode_at`) убраны из основного INSERT/UPDATE в `server/routes/anime.js`; теперь обновляются отдельным запросом после COMMIT с `try/catch` (graceful degradation — работает даже если миграция 006 ещё не применена). Исправлена светлая тема страницы уведомлений: все жёсткие цвета (`#13151c`, `#1c1f2a`, `#6b7280`, `#4b5563`, `#9ca3af`, `rgba(255,255,255,...)`) заменены на CSS-переменные (`var(--bg-surface)`, `var(--bg-elevated)`, `var(--border)`, `var(--text-primary/muted/faint/veryfaint)`). Улучшен дизайн: аватар с border и overflow:hidden, badge иконки типа, chip для new_episode с названием аниме и номером эпизода, hover на кнопке удаления (#f87171), спиннер загрузки.
- **2026-05-01 v10**: Добавлено расписание выхода серий для онгоингов. Миграция 006: поля `episodes_aired`, `air_weekday` (0=Пн…6=Вс), `air_time` (МСК), `next_episode_at` (UTC) в таблице `anime`; поле `episode_number` и `body` в `notifications`. Новый сервис `server/services/episodeScheduler.js`: запускается при старте сервера, каждые 5 минут проверяет онгоинги у которых `next_episode_at <= NOW()`, инкрементирует `episodes_aired`, вычисляет следующую дату (+7 дней), отправляет уведомления `type=new_episode` всем пользователям у которых аниме в списке «Смотрю», автоматически переводит аниме в `completed` при выходе последней серии. `AnimePage`: блок «Следующий эпизод» с обратным отсчётом в реальном времени (обновляется каждую секунду), датой в формате «5 мая 18:30 МСК», прогресс-баром вышедших серий — отображается только для онгоингов с заданным расписанием. `AnimeForm` (AdminPage): секция «Расписание серий» — день недели, время (МСК), счётчик вышедших серий; появляется только для статуса «Выходит». `NotificationBell` и `NotificationsPage`: тип `new_episode` с иконкой 📺 и текстом из поля `body`.
- **2026-05-01 v9**: Добавлен `<NotificationBell />` в шапку сайта (desktop — рядом с переключателем темы для авторизованных; mobile — ссылка «🔔 Уведомления» в меню). Переработан блок новостей на главной: featured-карточка для первой новости (горизонтальная, с обложкой), сетка 3 колонки для остальных, кнопка «Показать ещё» с пагинацией, счётчик в заголовке. Форма добавления новостей (модалка с полями заголовок/текст/URL обложки + live-превью) доступна модераторам и администраторам прямо на главной. `server/routes/news.js`: `requireAdmin` заменён на `requireModOrAdmin` (role 1 или 2). Roadmap: реализован Refresh Token — access token 15 мин + refresh token 30 дней с ротацией; `POST /api/auth/refresh` и `POST /api/auth/logout`; таблица `refresh_tokens` (миграция 005); авто-рефреш при 401 в `api.js` с защитой от гонок.
- **2026-04-30 v8**: Исправлены CommentBlock и NotificationBell для светлой темы: карточки комментариев используют `var(--bg-surface/elevated)` вместо `#13151c/rgba(255,255,255,0.025)`, все rgba(255,255,255,...) бордеры → `var(--border)`, textarea `text-white` → `var(--text-primary)`, кнопки тулбара, submit inactive state, onBlur border. NotificationBell: badge border `var(--header-bg)`, badge text явно белый, тень дропдауна облегчена, внутренние бордеры → `var(--border/border-light)`. Roadmap: добавлен PWA (`manifest.json`, `sw.js`, meta в index.html, регистрация в main.jsx), `robots.txt`, динамический `sitemap.xml` (Express роут `/sitemap.xml`), `Dockerfile` (multi-stage), `docker-compose.yml` (с PostgreSQL, volumes, healthcheck), graceful shutdown сервера (SIGTERM/SIGINT + db.end()), Express раздаёт dist/ в production + SPA fallback.
- **2026-04-29 v7**: Доработана светлая тема на страницах Admin, Anime, Character, Characters (каталог персонажей) и Profile. Во всех файлах жёстко прописанные тёмные цвета (#0d0f14, #13151c, #1c1f2a, #1a1d26) заменены на CSS-переменные (--bg-base/surface/elevated). Все rgba(255,255,255,...) бордеры → var(--border). Текстовые цвета (#9ca3af, #6b7280, #4b5563, #d1d5db) → var(--text-*). Аналогично исправлены компоненты CommentBlock, RatingsHistory, ReportModal, AddToCollection, NotificationBell. В index.css добавлен блок глобальных переопределений для html.light: text-white, input/textarea/select фоны, skeleton-shimmer, таблицы, toggle-переключатели, overlay-фоны. Градиент-оверлей на герое AnimePage адаптирован под var(--bg-base).
- **2026-04-29 v6**: Исправлен критический баг каталога — `load` useCallback имел `isNew` (нестабильная переменная из `searchParams`) и prefetch-эффект использовал `totalPages` до её объявления в области видимости, что приводило к краши компонента и отображению пустой страницы. Исправлено: убран `isNew` из deps, удалён проблемный prefetch. Исправлена светлая тема: логотип HUNT теперь тёмный, все жёстко прописанные цвета (`#9ca3af`, `#d1d5db`, `text-white`) в Layout/AnimeCard/Catalog заменены на CSS-переменные; скелетон адаптирован для светлого фона; кнопки фильтров используют `var(--bg-elevated)`.
- **2026-04-29 v5**: Исправлен баг: `ThemeToggle` был определён в `Layout.jsx`, но не рендерился — добавлен в правую часть шапки (desktop) и в мобильное меню. Все жёстко прописанные цвета (`#0d0f14`, `#1a1d26`) в Layout заменены на CSS-переменные (`--bg-base`, `--bg-elevated`, `--bg-surface`, `--header-bg`). Добавлена переменная `--header-bg` в обе темы в `index.css`. Добавлен тихий prefetch следующей страницы в `Catalog.jsx` (снижает задержку при переходе). Подтверждён health endpoint `/api/health` (уже был в server/index.js).
- **2026-04-27 v4**: Коллекции (списки/подборки) — таблицы `collections` и `collection_items` в БД (миграция 004), API `/api/collections` (CRUD, добавление аниме, публичный просмотр), UI `CollectionsPage.jsx` + компонент `AddToCollection.jsx`. Уведомления — таблица `notifications`, API `/api/notifications`, `NotificationBell.jsx` в шапке, страница `/notifications`; типы: `comment_reply`, `review_like`, `mention`, `new_review`.
- **2026-04-26 v3**: Публичный профиль — биография, баннер, местоположение, сайт, соцсети теперь отображаются другим пользователям; UI форм добавления авторов (StaffTab в AnimePage) и сэйю (SeiyuSection в CharacterPage) с поиском, созданием нового, выбором языка/роли.
- **2026-04-26 v2**: Исправлена ссылка на студию в AnimePage (каталог теперь открывается с фильтром по студии); жанры и темы на странице аниме стали кликабельными ссылками в каталог; Catalog.jsx теперь читает все фильтры из URL при инициализации (studio, genre, theme, status, type, season_year, season_quarter). Добавлены: расширенный профиль пользователя (bio, баннер, локация, сайт, соцсети) — миграция + бэк + UI; жалобы (reports) — модалка ReportModal в комментариях, роут /api/reports, вкладка в AdminPage; сэйю (API поиска/создания/привязки к персонажу); авторы аниме (staff) API; фильтр каталога по году и сезону выхода (season_year/quarter) — бэк + UI; рейтинг пользователей по активности (leaderboard) в AdminPage; очередь жалоб с обработкой в AdminPage; закрепление рецензий (is_pinned); миграция 003_stage4_features.sql.
- **2026-04-26**: Исправлена главная страница (Home.jsx не извлекал `.items` из API-ответа `{items,total}`). Добавлены: скелетоны (Skeleton.jsx) в каталоге и на главной; хук `usePageMeta` с динамическими title/description/OG для AnimePage, Home, Catalog, CharacterCatalog; удаление аккаунта (мягкое, с подтверждением паролем) на бэке и фронте; фильтр по роли и пагинация в каталоге персонажей; страница серии `/series/:id`; code splitting через `React.lazy`; индексы БД (`migrations/002_performance_indexes.sql`); настройка connection pool в db.js; SEO — `lang="ru"`, base meta-tags в index.html.
- **2026-04-25**: Добавлена серверная валидация через `joi` (регистрация, логин, аниме, комментарии, рецензии, оценки). Admin-панель расширена: управление пользователями (просмотр, смена роли, бан/разбан), CRUD жанров и студий, удаление аниме с каскадом, единая очередь модерации медиа. Добавлен роут `/api/admin` с отдельным middleware. SQL-audit завершён — все запросы используют параметризацию.

